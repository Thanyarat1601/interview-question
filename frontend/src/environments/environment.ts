// Used for dev (`ng serve`). Replaced at build time for production
// via the fileReplacements rule in angular.json.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api',
};
