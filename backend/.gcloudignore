# Files ignored when uploading source for Cloud Run / Cloud Build.
# NOTE: gitignore-style — bare `server` would match the cmd/server/ directory,
# which breaks the Docker build. Use leading slash for root-only matches.
.git
.gitignore
.gcloudignore
.env
*.log
*.md
tmp/
/server
/server.exe
